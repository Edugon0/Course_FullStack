//Operadores Logicos

// && (AND) -> Necessario as expressões precisam ser veradeiras para retorna "True", se caso tiver false no meio vai ficar considerar na saida "false"

    const user = "eduardo"
    const pasword = "123456"
    const login = user === "eduardo" && pasword ==="123456";

    console.log(login)

// || (OR) -> ele vai me retornar "True", pois não preciso que todas expressões ficam true, so apenas uma delas pra saida ser true

    console.log(true || false)

// ! (NOT) ->

console.log(!true)
console.log(! false)

